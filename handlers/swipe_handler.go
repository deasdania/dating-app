package handlers

import "github.com/labstack/echo/v4"

func (h *Handlers) Swipe(c echo.Context) error {
	//  HANDLE IF already meet the limit
	// return err only while if it's already reach the limit
	return nil
}
